import React, { Component } from "react";
import { withFirebase } from "../firebase-config";

class SchoolProfile extends Component {
  render() {
    return <div></div>;
  }
}

export default withFirebase(SchoolProfile);
